Task: 
Create a console application that uses Dependency Injection to manage dependencies. 
Define an ILogger interface with a Log method, implement it in the ConsoleLogger class, and inject the dependency via a constructor into the Application class, which will use ILogger to log messages.
